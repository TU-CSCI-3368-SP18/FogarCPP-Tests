--Author: Stephen Chang
--Case Name: Are you supposed to print sir statements?
--Output:
--"possible to choose"
--"possible to decide"
--"ok"
--Knave
--Knight
--End Output:

sir option1 the "I think we are supposed to"?
sir option2 the "I think we are not supposed to"?

sir chooseoption1 the Knight?
sir chooseoption2 the knave?

if (chooseoption1 both chooseoption2) then "impossible to decide" else "possible to choose"?
if (chooseoption1 one chooseoption2) then "possible to decide" else "impossible to choose"?

sir var120 the 20 + 4 * 5 ^ 2?
sir var9 the ~((232.23 - (23 // 3)) ^ 2) % 10?

if (var9 >= var120) then "wrong" else "ok"?
if (var9 < var120) then (sir foo the knave lord of (sir bar the knight lord of foo both bar)) else "boo"?
if (var9 < var120) then (sir foo the knave lord of (sir bar the knight lord of bar one (bar = foo))) else "boo"?
